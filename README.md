# Open Panel Docs

Open Panel 的 VitePress 中文文档站。

## 本地开发

环境要求：Node.js 22 或更高版本、pnpm 12。

```bash
pnpm install
pnpm dev
```

## 构建

```bash
pnpm build
pnpm preview
```

构建产物位于 `docs/.vitepress/dist`。站点使用相对基础路径，可直接部署到域名根路径或任意子目录。
